apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiops-platform.fullname" . }}
  labels:
    {{- include "aiops-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "aiops-platform.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
