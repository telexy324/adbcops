apiVersion: v1
kind: Secret
metadata:
  name: {{ include "aiops-platform.fullname" . }}-secret
  labels:
    {{- include "aiops-platform.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
